{
  "name": "chunxi-road-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Chunxi Road multimodal dashboard and grounded AI chat.",
  "scripts": {
    "build-data": "python scripts/build_dashboard_data.py",
    "build-recommendations": "node scripts/build_recommendation_cache.js --provider local",
    "start": "node server.js"
  }
}
